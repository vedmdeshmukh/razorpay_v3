# Architecture

Gateway/API response + webhook -> state machine -> evidence/rules + ML advisor -> safety validation -> recovery -> reconciliation.

ML is advisory only. Financial truth comes from authoritative evidence.
