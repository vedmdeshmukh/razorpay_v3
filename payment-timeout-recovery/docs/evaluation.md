# Evaluation

Compare a deterministic rule-only baseline against the rule + ML advisor system on the same simulated failure scenarios. Track resolution time, recovery success, duplicate operations, incorrect actions, reconciliation rate, and fallback behavior.
